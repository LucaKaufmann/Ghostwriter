# Memory

## Tests Before PRs
- Always run relevant tests locally before creating a PR.

## How To Run Tests
- iOS: `cd EpilogueIOS && tuist install && tuist test`
- Android: `./gradlew test`
- Ghostwriter: `cd ghostwriter && pytest`
