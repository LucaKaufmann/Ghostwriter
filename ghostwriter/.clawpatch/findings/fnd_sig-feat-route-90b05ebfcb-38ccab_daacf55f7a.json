{
  "schemaVersion": 1,
  "findingId": "fnd_sig-feat-route-90b05ebfcb-38ccab_daacf55f7a",
  "featureId": "feat_route_90b05ebfcb",
  "title": "Non-file audio paths under the podcast directory return a server error",
  "category": "bug",
  "severity": "low",
  "confidence": "high",
  "triage": "confirmed-bug",
  "evidence": [
    {
      "path": "app/api/podcast.py",
      "startLine": 698,
      "endLine": 705,
      "symbol": "download_podcast_episode",
      "quote": "if not episode.audio_path or not os.path.exists(episode.audio_path):\n        raise HTTPException(status_code=404, detail=\"Audio file not found\")\n\n    audio_path = _validated_child_path(\n        Path(episode.audio_path),\n        allowed_parent=Path(settings.output_dir) / \"podcasts\",\n        not_found_detail=\"Audio file not found\",\n    )"
    },
    {
      "path": "app/api/podcast.py",
      "startLine": 223,
      "endLine": 235,
      "symbol": "_validated_child_path",
      "quote": "def _validated_child_path(path: Path, *, allowed_parent: Path, not_found_detail: str) -> Path:\n    \"\"\"Resolve and validate file path remains inside an expected parent directory.\"\"\"\n    try:\n        resolved = path.resolve(strict=True)\n    except OSError as exc:\n        raise HTTPException(status_code=404, detail=not_found_detail) from exc\n\n    parent_resolved = allowed_parent.resolve()\n    try:\n        resolved.relative_to(parent_resolved)\n    except ValueError as exc:\n        raise HTTPException(status_code=403, detail=\"Invalid media file path\") from exc\n    return resolved"
    },
    {
      "path": "app/api/podcast.py",
      "startLine": 707,
      "endLine": 711,
      "symbol": "download_podcast_episode",
      "quote": "return FileResponse(\n        audio_path,\n        media_type=\"audio/mpeg\",\n        filename=f\"podcast-{episode_id}.mp3\",\n    )"
    }
  ],
  "reasoning": "The route only checks that the configured path exists and is inside the podcasts directory. A directory or other non-regular file inside that directory passes validation and is handed to FileResponse, which raises at response time instead of returning a controlled 404/409. This turns a bad persisted audio_path into a 500.",
  "reproduction": "Create a ready PodcastEpisode whose audio_path is the podcasts output directory itself, then GET /api/podcast/episodes/{id}/download with valid auth. The path exists and passes _validated_child_path, but FileResponse cannot serve a directory.",
  "recommendation": "After resolving the child path, require audio_path.is_file() before constructing FileResponse and raise the existing 404 Audio file not found detail if it is not a regular file.",
  "whyTestsDoNotAlreadyCoverThis": "The existing podcast API test covers an outside-the-directory path guard, but not an existing non-file path inside the allowed podcast directory.",
  "suggestedRegressionTest": "Add a download test with audio_path set to the podcasts directory and assert the route returns a controlled 404 rather than a 500.",
  "minimumFixScope": "Add a regular-file check in download_podcast_episode after _validated_child_path returns.",
  "status": "open",
  "history": [],
  "signature": "sig_feat-route-90b05ebfcb_38ccabd067",
  "linkedPatchAttemptIds": [],
  "createdByRunId": "20260518T120435-a94ff3",
  "createdAt": "2026-05-18T12:22:40.351Z",
  "updatedAt": "2026-05-18T12:22:40.351Z"
}
